<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- أدمن فقط -->
    <item android:id="@+id/menu_admin"
        android:title="🛡️ لوحة الإدارة"
        app:showAsAction="never"
        android:visible="false"/>

    <!-- أدمن + مديرون -->
    <item android:id="@+id/menu_members"
        android:title="👥 إدارة الأعضاء"
        app:showAsAction="never"
        android:visible="false"/>

    <!-- أدمن فقط -->
    <item android:id="@+id/menu_managers"
        android:title="⭐ إدارة المديرين"
        app:showAsAction="never"
        android:visible="false"/>

    <!-- للجميع -->
    <item android:id="@+id/menu_abuse_reports"
        android:title="🚨 بلاغات سوء الاستخدام"
        app:showAsAction="never"
        android:visible="false"/>

    <!-- للجميع -->
    <item android:id="@+id/menu_about"
        android:title="ℹ️ عن التطبيق"
        app:showAsAction="never"/>

    <item android:id="@+id/menu_disclaimer"
        android:title="📋 الحقوق والشروط"
        app:showAsAction="never"/>

    <!-- للمسجلين -->
    <item android:id="@+id/menu_logout"
        android:title="🚪 تسجيل الخروج"
        app:showAsAction="never"
        android:visible="false"/>

    <!-- للزوار -->
    <item android:id="@+id/menu_login"
        android:title="🔑 تسجيل الدخول"
        app:showAsAction="never"/>
</menu>
